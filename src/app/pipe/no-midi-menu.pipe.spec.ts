import { NoMidiMenuPipe } from './no-midi-menu.pipe';

describe('NoMidiMenuPipe', () => {
  it('create an instance', () => {
    const pipe = new NoMidiMenuPipe();
    expect(pipe).toBeTruthy();
  });
});
